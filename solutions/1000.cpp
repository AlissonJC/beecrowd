/*
 * Beecrowd 1000 - Hello World!
 *
 * The classic first program: print the message "Hello World!" followed by an
 * end-of-line. The problem has no input.
 *
 * Input : none.
 * Output: the single line "Hello World!".
 *
 * Approach: one print statement with the exact text and a trailing newline.
 *
 * Complexity: O(1) time and space.
 */

#include <cstdio>

int main() {
    printf("Hello World!\n");        // exact message, including the trailing newline
    return 0;
}
